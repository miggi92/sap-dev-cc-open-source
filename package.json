{
    "license": "MIT",
    "private": true,
    "scripts": {
      "unit": "rm -rf output && abap_transpile abap_transpile.json && echo RUNNING && node output/index.mjs",
      "test": "npm run unit",
      "abaplint": "abaplint --format codeframe"
    },
    "repository": {
      "type": "git",
      "url": "github:miggi92/sap-dev-cc-open-source"
    },
    "devDependencies": {
      "@abaplint/cli": "^2.100.3",
      "@abaplint/runtime": "^2.7.0",
      "@abaplint/transpiler-cli": "^2.7.1"
    }
  }